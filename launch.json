{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(lldb) Launch",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/cli/plut_cli",
            "args": ["${workspaceFolder}/build/example/libplut_example.so"],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "runInTerminal": true,
			"sudo": true
        }


    ]
}
